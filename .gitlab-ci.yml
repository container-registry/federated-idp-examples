stages:
  - build

build-and-push:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  id_tokens:
    ID_TOKEN:
      aud: macfly4200.8gears.ch  # 👈 audience claim matching your registry
  variables:
    BUILDKIT_HOST: tcp://buildkitd:1234
  before_script:
    - apk add --no-cache curl jq
  script:
    - echo "Using GitLab OIDC token"

    # Pretty print the JWT token (header and payload)
    - echo "=== JWT Header ==="
    - |
      HEADER=$(echo "$ID_TOKEN" | cut -d'.' -f1 | tr '_-' '/+')
      HEADER_PADDED="${HEADER}$(printf '%*s' $((4 - ${#HEADER} % 4)) '' | tr ' ' '=')"
      echo "$HEADER_PADDED" | base64 -d | jq .
    - echo ""
    - echo "=== JWT Payload ==="
    - |
      PAYLOAD=$(echo "$ID_TOKEN" | cut -d'.' -f2 | tr '_-' '/+')
      PAYLOAD_PADDED="${PAYLOAD}$(printf '%*s' $((4 - ${#PAYLOAD} % 4)) '' | tr ' ' '=')"
      echo "$PAYLOAD_PADDED" | base64 -d | jq .

    # Login to registry using JWT token
    - echo "$ID_TOKEN" | docker login -u not-relevant --password-stdin macfly4200.8gears.ch

    # Build image
    - docker buildx create --use --name remote --driver remote ${BUILDKIT_HOST}
    - docker buildx -t macfly4200.8gears.ch/library/image:$CI_COMMIT_SHA .

    # Pull image from registry
    - docker pull macfly4200.8gears.ch/library/hello-world:latest
  rules:
    - when: manual
